package sequentialSolution;

/**
 * The main entry point for the sequential solution.
 */
public class SequentialMain {
    /**
     * The main method to start the sequential application.
     *
     * @param args Command-line arguments.
     */
    public static void main(String[] args) {
        SeqUI seqUI = new SeqUI();
        seqUI.run(args);
    }
}