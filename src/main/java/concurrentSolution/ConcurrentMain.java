package concurrentSolution;

/**
 * The main entry point for the concurrent solution.
 */
public class ConcurrentMain {
    /**
     * The main method to start the concurrent application.
     *
     * @param args Command-line arguments.
     */
    public static void main(String[] args) {
        ConUI conUI = new ConUI();
        conUI.run(args);
    }
}